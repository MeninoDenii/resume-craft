import { cn } from "@/lib/utils";

export const ResumeContent = () => {
  return (
    <section
      className={cn(
        "overflow-hidden w-full h-full flex items-center justify-center",
        "relative bg-muted dark:bg-background"
      )}
    ></section>
  );
};
